    To start the application
1. install maven 
2. build project using maven run command mvn clean install
3. using in memory database
   1. to login into database use url: http://localhost:8086/h2

4.run the application using mvn spring-boot:run
5.once ran the application using step 4. visit http://localhost:8086/swagger-ui.html
to see all the api
